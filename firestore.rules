rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend server manages logic via API routes
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
